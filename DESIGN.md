# 클리핑 판형 규격

`_template.html` = 디자인 기준. 매 발행 시 이 파일을 복사해 내용만 교체할 것.
(기존 TDM MI 대시보드와 동일한 디자인 시스템)

## 구조
- `.app` (max 1320px) → `.app-header` (다크 그라데이션 + 우측 quick 지표 4개) → `.tabs` → `.tab-pane`
- **탭 4개**: 📊 요약 / 🎬 유튜브 / 📰 뉴스·정책 / 💬 커뮤니티

## 탭별 구성
| 탭 | 내용 |
|---|---|
| 요약 | `.kpi-row` 4칸 + 핵심 요약 `.note` + 오늘의 시사점 `.note.warn` |
| 유튜브 | `.two-col`(경쟁사 채널 / 인플루언서 랭킹) → 이슈 심층 `.deep` → `.two-col`(자사 언급 레이더 / 쇼츠) |
| 뉴스·정책 | `.two-col` × 2 = **자사 / 상용차 산업 / 경쟁사 / 정책·환경** (기존 뉴스클리핑 4분면 유지) |
| 커뮤니티 | `.two-col` = 자사 관련 / 경쟁사·시장 |

## 컴포넌트
- `.news-item` — 뉴스·채널·언급 항목 카드 (title 링크 / desc / meta)
- `.deep` — 이슈 심층: `.dh`(제목+통계) + `.db`(`.q` 댓글 인용 + `.verdict` 판독)
- `.tag` — neg(부정) / pos(긍정) / comp(경쟁) / neu(중립) / pol(정책) / hot(확산)
- `.status` — on(공세) / off(공백·휴면) / idle(저활동)

## 규칙
- 모든 콘텐츠에 링크 필수. 영상 `youtube.com/watch?v=`, 쇼츠 `youtube.com/shorts/`
- 헤더 quick 지표: 신호등 / 최대 확산 / 자사 언급 수 / 모니터링 채널 수
- 신호등: 🟢평온 · 🟡주목 · 🔴이슈
